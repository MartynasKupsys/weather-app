{
  "name": "frontend",
  "version": "1.0.0",
  "description": "nodeJS Javascript project",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "dead-server --port=3000 --host=localhost"
  },
  "author": "Martynas",
  "license": "ISC",
  "dependencies": {
    "dead-server": "^1.0.6"
  }
}
